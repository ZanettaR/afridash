import React, {Component} from 'react'
import {browserHistory } from 'react-router-dom'
export class AboutMe extends Component {
  render() {
    return (
      <div className="container">
        <h2 className="btn btn-danger">About Page</h2>
      </div>
    );
  }
}

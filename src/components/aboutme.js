import React, {Component} from 'react'
import {browserHistory } from 'react-router-dom'
import {Header} from './header'
import {Home} from './home'
export class AboutMe extends Component {
  render() {
    return (
      <div>
    <div id="page-wrapper"><p>Hello World</p>
  </div>
    </div>
    );
  }
}
